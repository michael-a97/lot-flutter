name: api
version: 0.0.1
publish_to: none
environment:
  sdk: ^3.7.0
description: A package for interacting with the lot backend
dependencies:
  dartz: ^0.10.1
  dio: ^5.8.0+1
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  meta: ^1.16.0
  mocktail: ^1.0.4
  networkx: ^0.2.1
  pretty_dio_logger: ^1.4.0
  session_storage:
    path: '../session_storage'
  test: ^1.25.15
dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  pubspec_dependency_sorter: ^1.0.5
