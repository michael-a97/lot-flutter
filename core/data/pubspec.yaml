name: data
version: 0.0.1
publish_to: none
environment:
  sdk: ^3.7.0
description: A new Flutter package project.
dependencies:
  api:
    path: ../api
  config:
    path: ../config
  dartz: ^0.10.1
  database:
    path: ../database
  dtos:
    path: ../dtos
  equatable: ^2.0.7
  firebase_auth: ^5.5.2
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  injectable: ^2.5.0
  secure_storage:
    path: ../secure_storage
dev_dependencies:
  build_runner: ^2.4.15
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0
  mocktail: ^1.0.4
  pubspec_dependency_sorter: ^1.0.5
  test: ^1.25.15
